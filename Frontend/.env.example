# StageX Frontend — environment configuration
# Copy to `.env` for local dev. This is the ONE place to point the app at a
# different backend. Everything routes through src/config/index.js.

# Base URL of the StageX backend API.
VITE_API_BASE_URL=http://localhost:8080
